{% if menu %}
  <ul class="navbar-nav ml-auto mt-2 mt-lg-0">
	{% for item in menu %}
		<li class="{{item.classes | merge(['nav-item']) | join(' ')}}">
			<a class="nav-link" href="{{item.get_link}}">{{item.title}}</a>
		</li>
	{% endfor %}
	</ul>
{% endif %}
